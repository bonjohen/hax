# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**hax** — new project, currently empty. The `sdlc/docs/` directory is set up for the five-stage design workflow (design doc → PDR → phased plan → execute → commit).

## Repository Structure

```
sdlc/docs/      # Design documents, PDRs, and phased plans
```

## Build / Test / Lint

No build system, test framework, or linter is configured yet. Update this section when tooling is added.
